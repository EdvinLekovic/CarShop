const backendServer = 'http://localhost:9090'

export const carsPage = backendServer + '/cars';

export const userResource = backendServer + '/users';

export const paymentResource = backendServer + '/payment';

export const serviceResource = backendServer + '/service-request';

export const reviewResource = backendServer + '/reviews';
